{
  "name": "notebackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "\"echo \"Error: no test specified\" && exit 1\"",
    "build:ui": "rm -rf dist && cd ../testApp/ && npm run build && cp -r dist ../NodeApp_backend/",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs",
    "lint": "eslint ."
  },
  "author": "Julia Valkama",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.1.0",
    "mongoose": "^9.2.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.3",
    "@flydotio/dockerfile": "^0.7.10",
    "@stylistic/eslint-plugin": "^5.9.0",
    "eslint": "^9.39.3",
    "globals": "^17.4.0",
    "nodemon": "^3.1.9"
  }
}
